---
name: TreeFromDB


Model::TPath:
  schema_class: Schema::TPath
  connect_info:
    dsn: 'dbi:SQLite:dbname=:memory:'

#Model::TPath:
#  schema_class: Schema::TPath
#  connect_info:
#    dsn: dbi:mysql:database=tpath;host=localhost
#    user: root
#    password: root

TreePath:
  debug: 0
  backend:
    name: DBIx
    args:
      model: Model::TPath
      source_name: Page
